
void    ui_prefab_get_relative_size(t_ui_el *p, t_ui_el *canvas, int type, t_fvec2 *size)

принимает в себя родительский элемент для переданой позиции, канвас, флаг обозначающий тип размера, размер.

переводит размер из любых заданых типов (в пикселях, в процентах от канваса, в процентах от родителя) в тип "в процентах от родителя".

Нужна для удобного создания префабов.

Полность не тестировалась.
