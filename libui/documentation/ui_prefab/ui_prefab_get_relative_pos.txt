
void    ui_prefab_get_relative_pos(t_ui_el *p, t_ui_el *canvas, int type, t_fvec2 *pos)

принимает в себя родительский элемент для переданой позиции, канвас, флаг обозначающий тип позиции, позицию.

переводит позицию из любых заданых типов (в пикселях от канваса, в процентах от канваса, в пикселях от родителя, в процентах от родителя) в тип "в процентах от родителя".

Нужна для удобного создания префабов.

Полностью не тестировалась.
